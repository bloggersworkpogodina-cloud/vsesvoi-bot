# Vsesvoi Bot V3.1.2 Real Fix

Что исправлено:
- безопасные короткие `event_id/link_id` для Telegram deep-links и callback-кнопок;
- старым мероприятиям с кириллицей/длинным ID автоматически добавляется безопасный `link_id`;
- карточка мероприятия открывается через безопасный token;
- регистрации по старым мероприятиям резолвятся через `link_id`;
- PostgreSQL остается основным хранилищем.

После загрузки:
1. Commit changes в GitHub.
2. Дождаться Railway deploy.
3. Открыть `/admin` → ⚙️ Система, проверить PostgreSQL.
4. Создать новое тестовое мероприятие.
5. Перейти по ссылке регистрации и проверить, что участник отображается в карточке.
