# Все свои CRM — V3.1.1 PostgreSQL Hotfix

Это безопасное обновление на основе текущего рабочего `bot.py`.

Главное:
- не загружать старые архивы V4;
- загружать только эти 5 файлов;
- данные хранятся в PostgreSQL через `DATABASE_URL`;
- структура репозитория остается прежней: 5 файлов.

Проверка после деплоя:
1. `/admin`
2. `⚙️ Система` — должно быть `Хранилище: PostgreSQL`
3. создать тестовое мероприятие
4. открыть `📅 Мероприятия`
5. снова нажать Deploy в Railway и проверить, что мероприятие осталось
